/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // Uncomment and set if deploying to a subpath:
  // basePath: '/website',
  // assetPrefix: '/website/',
};

export default nextConfig;
